spring:
  datasource:
    username: tsgotkiabl
    password: xKC$bJlVwYHbZoVp
    url: ${DB_URL:jdbc:postgresql://hero-clinic-server.postgres.database.azure.com:5432/postgres}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
  flyway:
    enabled: false
app:
  jjwt:
    secret: ThisIsSecretForJWTHS512SignatureAlgorithmThatMUSTHave512bitsKeySize
    access_expiration: 86400
    refresh_expiration: 2592000
    tokenType: Bearer

