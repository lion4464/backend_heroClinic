spring:
  datasource:
    username: hero_clinic
    password: 6S(NwyH(q
    url: ${DB_URL:jdbc:postgresql://clinic.postgres.database.azure.com:5432/herodb}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
  flyway:
    enabled: false
app:
  jjwt:
    secret: ThisIsSecretForJWTHS512SignatureAlgorithmThatMUSTHave512bitsKeySize
    access_expiration: 86400
    refresh_expiration: 2592000
    tokenType: Bearer

